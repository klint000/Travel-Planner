<Window x:Class="Travel_Planner.CatalogPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Travel Planner - Каталог поездок"
        Height="760"
        Width="1220"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,18">
            <TextBlock Text="Каталог поездок" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,0" HorizontalAlignment="Left"/>
            <Button Content="Выйти"
                    DockPanel.Dock="Right"
                    Width="100"
                    Height="38"
                    VerticalAlignment="Center"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="12,0,0,0"
                    Click="LogoutButton_Click"/>
        </DockPanel>

        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="10"
                BorderBrush="#FFD1D9E0"
                BorderThickness="1"
                Padding="12"
                Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="230"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="190"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Добавить" Width="100" Height="36" Margin="0" Click="AddButton_Click"/>
                <Button Grid.Column="1" Content="Редактировать" Width="120" Height="36" Margin="10,0,0,0" Click="EditButton_Click"/>
                <Button Grid.Column="2" Content="Удалить" Width="100" Height="36" Margin="10,0,0,0" Click="DeleteButton_Click"/>

                <TextBox Grid.Column="4"
                         x:Name="SearchTextBox"
                         VerticalAlignment="Center"
                         Height="36"
                         ToolTip="Поиск по названию"
                         TextChanged="FilterOrSortChanged"/>

                <ComboBox Grid.Column="6"
                          x:Name="DifficultyFilterComboBox"
                          VerticalAlignment="Center"
                          Height="36"
                          SelectionChanged="FilterOrSortChanged">
                    <ComboBoxItem Content="Все" IsSelected="True"/>
                    <ComboBoxItem Content="Легко"/>
                    <ComboBoxItem Content="Средне"/>
                    <ComboBoxItem Content="Сложно"/>
                </ComboBox>

                <ComboBox Grid.Column="8"
                          x:Name="SortFieldComboBox"
                          VerticalAlignment="Center"
                          Height="36"
                          SelectionChanged="FilterOrSortChanged">
                    <ComboBoxItem Content="Название" IsSelected="True"/>
                    <ComboBoxItem Content="Дата начала"/>
                    <ComboBoxItem Content="Бюджет"/>
                </ComboBox>

                <ComboBox Grid.Column="10"
                          x:Name="SortDirectionComboBox"
                          VerticalAlignment="Center"
                          Height="36"
                          SelectionChanged="FilterOrSortChanged">
                    <ComboBoxItem Content="По возрастанию" IsSelected="True"/>
                    <ComboBoxItem Content="По убыванию"/>
                </ComboBox>
            </Grid>
        </Border>

        <ListView Grid.Row="2"
                  x:Name="CatalogListView"
                  Background="Transparent"
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0,0,16,16"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Width="270"
                            Height="350"
                            Background="{StaticResource SurfaceBrush}"
                            CornerRadius="12"
                            BorderBrush="#FFD1D9E0"
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="165"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="#FFEAF4F7">
                                <Image Stretch="UniformToFill"
                                       Source="{Binding Image}"/>
                            </Border>

                            <StackPanel Grid.Row="1" Margin="14,12,14,14">
                                <TextBlock Text="{Binding Title}"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           FontWeight="SemiBold"
                                           FontSize="16"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           FontSize="13"
                                           Margin="0,6,0,10"
                                           TextWrapping="Wrap"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="★"
                                               Foreground="{StaticResource SecondaryAccentBrush}"
                                               FontSize="14"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Rating, StringFormat=Рейтинг: {0}/5}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontSize="13"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="⏱"
                                               Foreground="{StaticResource PrimaryAccentBrush}"
                                               FontSize="14"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Duration}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontSize="13"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="⚑"
                                               Foreground="{StaticResource PrimaryAccentBrush}"
                                               FontSize="14"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Difficulty}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontSize="13"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Budget}"
                                           Foreground="{StaticResource SecondaryAccentBrush}"
                                           FontWeight="SemiBold"
                                           FontSize="14"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
